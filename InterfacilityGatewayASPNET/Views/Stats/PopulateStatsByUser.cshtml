@model List<InterfacilityGatewayASPNET.dto.HandlerDto>
@using System.Web.Mvc.Html
@{
    ViewBag.Title = "Index";
    //Layout = "~/Views/Shared/_Layout.cshtml";
    Layout = "~/Views/Shared/_LayoutPageTry2.cshtml";
}




<h2>Your Admissin(s):</h2>

<hr />
@if (!Model.Any())
{
    <p>You have not admitted any patient yet.</p>
}

else
{
    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                <th>Request Number</th>
                <th>Name</th>
                <th>Eligibility</th>
                <th>Urgency</th>
                <th>Date</th>

            </tr>
        </thead>
        <tbody>
            @foreach (var HandlerDto in Model)
            {
                <tr>



                    <td>@Html.ActionLink(HandlerDto.IdHandlerDto, "Details", "HandlerTransferRequest", new { id = HandlerDto.HandlerTransferRequestHandlerDto.Id }, null)</td>
                    <td>@Html.ActionLink(HandlerDto.ClientReportHandlerDto.FullName, "Details", "HandlerTransferRequest", new { id = HandlerDto.HandlerTransferRequestHandlerDto.Id }, null)</td>
                    <td>@Html.ActionLink(HandlerDto.EligibilityStatusHandlerDto, "Details", "HandlerTransferRequest", new { id = HandlerDto.HandlerTransferRequestHandlerDto.Id }, null)</td>
                    <td>@Html.ActionLink(HandlerDto.CategoryHandlerDto, "Details", "HandlerTransferRequest", new { id = HandlerDto.HandlerTransferRequestHandlerDto.Id }, null)</td>
                    <td>@Html.ActionLink(HandlerDto.RequestDateHandlerDto, "Details", "HandlerTransferRequest", new { id = HandlerDto.HandlerTransferRequestHandlerDto.Id }, null)</td>


                </tr>
            }
        </tbody>
    </table>
}